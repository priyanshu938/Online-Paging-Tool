<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Paging</title>
    <link rel="stylesheet" href="list.css">
</head>

<body>
    <h1>
        <%=type%> PAGE REPLACEMENT ALGORITHM
    </h1>

    <div>
        <h3 style="    margin-top: 80px;">Page Reference String :<br><br>
            <%=reference_string %> <br>
        </h3>
        <h3>Number of Frames: <%=number_of_frames %>
        </h3>
        <h2>OUTPUT:</h2>
        <table>
            <tr>
                <% for (var i=0; i <number_of_frames; i++){ %>
                    <% for (var j=0; j <length_of_string; j++) {%>
                        <% if(string_frames[i][j]==-1){ %>
                            <td> </td>
                            <% } %>

                                <% if(string_frames[i][j]!=-1){ %>
                                    <td><button>
                                            <%=string_frames[i][j] %>
                                        </button></td>
                                    <% } %>

                                        <% } %>
            </tr>

            <% } %>

                <tr>
                    <% for(var i=0;i<length_of_string;i++) {%>

                        <% if(hit_string[i]==" " ) { %>
                            <td> </td>
                            <% } %>
                                <% if(hit_string[i]!=" " ){ %>
                                    <td> <button style="background-color: brown;">
                                            <%=hit_string[i] %>
                                        </button></td>
                                    <% } %>

                                        <% } %>
                </tr>
        </table>
        <h3>Number of page faults : <%=length_of_string-count %>
        </h3>
        <h3>Number of page hits : <%=count %>
        </h3>
        <br>
        <form action="/list" method="post">
            <button class="back" type="submit">Back</button>
        </form>

    </div>
</body>

</html>